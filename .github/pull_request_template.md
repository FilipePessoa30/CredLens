## Summary

<!-- What does this PR change, and why? Focus on the "why" — the diff already shows the "what". -->

## Related phase

<!-- Which docs/roadmap.md phase does this belong to? Link the relevant doc section if applicable. -->

## Checklist

- [ ] Change is scoped to the current phase (`docs/roadmap.md`); any scope exception is called out below.
- [ ] No real data, credentials, or personal information were added (see `SECURITY.md`).
- [ ] No result, metric, or claim is presented that wasn't actually computed/executed.
- [ ] `uv run ruff check .` passes.
- [ ] `uv run ruff format --check .` passes.
- [ ] `uv run mypy src tests` passes.
- [ ] `uv run pytest` passes, with tests added/updated for the change.
- [ ] Documentation updated if this PR changes what's implemented vs. planned (`README.md`, `README.pt-BR.md`, or `docs/`).

## Scope exceptions / notes

<!-- Anything reviewers should know that doesn't fit above — e.g., a deliberate deviation from the current phase's scope, or a known limitation of this change. -->
